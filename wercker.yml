build:
  box: google/golang
  steps:
    - internal/docker-build:
        dockerfile: Dockerfile
        image-name: my-new-image
        build-args: "AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID} AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY} AWS_DEFAULT_REGION=${AWS_DEFAULT_REGION}"

