build:
  box: ubuntu
  steps:
    - internal/docker-build:
        dockerfile: Dockerfile
        image-name: my-new-image
    - internal/docker-run:
        image: my-new-image
        name: myTestContainer

